sudo: required
services:
- docker
cache: ccache
language: generic
notifications:
  email:
    recepients:
      - dengler@toposens.com

before_script:
  - docker pull toposens/toposens
  - docker run toposens/toposens bin/sh -c "source ros_setup.bash"
  - echo "test echo"
  - docker images
  - docker ps -a

script:
  - source ros_setup.bash
  - echo "holy guacamolee"
