sudo: required
services:
- docker
cache: ccache
language: generic
notifications:
  email:
    recepients:
      - dengler@toposens.com

before_script:
  - docker pull toposens/toposens

script:
  - docker run toposens/toposens
  - docker exec -ti $(docker ps -aq) sh -c "ls /opt/ros/"
  - source ros_setup.bash
  - echo "holy guacamolee"
