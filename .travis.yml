sudo: required
services:
- docker
cache: ccache
language: generic
notifications:
  email:
    recepients:
      - dengler@toposens.com

before_script:
  - docker pull toposens/toposens
  - docker run -d -p 127.0.0.1:80:4567 toposens/toposens /bin/sh -c "cd /root/toposens; bundle exec foreman start;"

script:
  - source ros_setup.bash
  - echo "holy guacamolee"
