sudo: required
services:
- docker
cache: ccache
language: generic
notifications:
  email:
    recepients:
      - dengler@toposens.com

before_script:
  - docker pull toposens/toposens

script:
  - docker run -it -p 8080:80 toposens/toposens
  - source ros_setup.bash
  - echo "holy guacamolee"
