sudo: required
services:
- docker
cache: ccache
language: generic

notifications:
  email:
    recepients:
      - dengler@toposens.com

env:
  - DOCKER_IMAGE=toposens/toposens
  - SCRIPT=travis.sh

before_script:
  - ln -s . .travis_test

script:
  - .travis_test/$SCRIPT
