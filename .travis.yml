sudo: required
services:
- docker
cache: ccache
language: generic
notifications:
  email:
    recepients:
      - dengler@toposens.com

before_script:
  - docker pull toposens/toposens
  - docker run -it toposens/toposens bash

script:
  - source ros_setup.bash
  - echo "holy guacamolee"
